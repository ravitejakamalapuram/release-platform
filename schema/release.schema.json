{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/ravitejakamalapuram/release-platform/main/schema/release.schema.json",
  "title": "release.yaml",
  "description": "Release contract for an app repo consumed by ravitejakamalapuram/release-platform.",
  "type": "object",
  "additionalProperties": false,
  "required": ["app", "targets"],
  "properties": {
    "app": {
      "type": "string",
      "description": "Short slug used in artifact names and the dashboard.",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$",
      "patternHint": "letters, digits, dot, dash, underscore"
    },
    "test": {
      "type": "string",
      "minLength": 1,
      "description": "Shell command run in the repo root before anything is built, uploaded or tagged. Non-zero exit aborts the release."
    },
    "targets": {
      "type": "array",
      "minItems": 1,
      "items": { "oneOf": [{ "$ref": "#/$defs/chrome" }, { "$ref": "#/$defs/android" }] }
    }
  },
  "$defs": {
    "relativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?!.*(^|/)\\.\\.(/|$)).+$",
      "patternHint": "a relative path inside the repo without '..'"
    },
    "chrome": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "item_id", "path"],
      "properties": {
        "type": { "const": "chrome" },
        "item_id": {
          "type": "string",
          "pattern": "^[a-p]{32}$",
          "patternHint": "the 32-letter Chrome Web Store item id"
        },
        "path": { "$ref": "#/$defs/relativePath", "description": "Directory containing manifest.json after the build step." },
        "build": { "type": "string", "minLength": 1, "description": "Optional build command, run in the repo root." },
        "node": { "type": "string", "pattern": "^\\d+(\\.\\d+){0,2}(\\.x)?$", "patternHint": "e.g. '22'" },
        "include": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 },
          "description": "Allowlist of files/dirs/globs (relative to path). Default: everything minus common junk."
        },
        "publish": {
          "type": "boolean",
          "description": "Submit for review after upload (default true). false = upload a draft only."
        }
      }
    },
    "android": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "package"],
      "properties": {
        "type": { "const": "android" },
        "package": {
          "type": "string",
          "pattern": "^[a-zA-Z][a-zA-Z0-9_]*(\\.[a-zA-Z][a-zA-Z0-9_]*)+$",
          "patternHint": "an Android application id like com.example.app"
        },
        "gradle_task": { "type": "string", "pattern": "^[A-Za-z0-9:_-]+$" },
        "build": {
          "type": "string",
          "minLength": 1,
          "description": "Custom build command instead of ./gradlew <gradle_task> (e.g. Flutter). VERSION_NAME and VERSION_CODE are exported."
        },
        "flutter": { "type": "string", "pattern": "^\\d+\\.\\d+(\\.\\d+)?(\\.x)?$", "description": "Install this Flutter version before building." },
        "aab": { "$ref": "#/$defs/relativePath" },
        "java": { "type": "string", "pattern": "^\\d+$", "patternHint": "e.g. '17'" },
        "track": { "type": "string", "minLength": 1, "description": "Play track to upload to (default internal)." },
        "release_status": {
          "enum": ["completed", "draft"],
          "description": "completed (default) rolls out to the track; draft is required while the app itself is still a draft in Play Console."
        },
        "release_notes": { "$ref": "#/$defs/relativePath", "description": "Markdown changelog; its first section becomes the en-US 'What's new' text." },
        "signing": {
          "type": "object",
          "additionalProperties": false,
          "description": "Names of the GitHub secrets (in the app repo) that hold the upload key.",
          "properties": {
            "keystore_base64": { "$ref": "#/$defs/secretName" },
            "keystore_password": { "$ref": "#/$defs/secretName" },
            "key_alias": { "$ref": "#/$defs/secretName" },
            "key_password": { "$ref": "#/$defs/secretName" }
          }
        }
      }
    },
    "secretName": {
      "type": "string",
      "pattern": "^[A-Za-z_][A-Za-z0-9_]*$",
      "patternHint": "a GitHub secret name"
    }
  }
}
